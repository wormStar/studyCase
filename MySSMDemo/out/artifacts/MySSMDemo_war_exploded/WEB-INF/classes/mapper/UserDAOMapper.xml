<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.stu.dao.UserDAO">

</mapper>
    <!--<select id="getByroleIdName" resultType="permission">
        select * from t_permission p join t_role_permission rp
        on p.id = rp.permission_id
        and rp.role_id = #{roleId} and p.name = #{name}
    </select>-->
